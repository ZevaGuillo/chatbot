/**
 * Prefijo usado para todas las claves relacionadas con el chatbot en localStorage.
 */
export const localStorageNamespace = 'n8n-chat';

/**
 * Clave usada para almacenar el ID de sesión actual del chat en localStorage.
 */
export const localStorageSessionIdKey = `${localStorageNamespace}/sessionId`;
